import { ArticleTextFilterPipe } from './article-text-filter.pipe';

describe('ArticleTextFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleTextFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
