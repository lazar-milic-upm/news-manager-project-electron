import { ArticleCategoryFilterPipe } from './article-category-filter.pipe';

describe('ArticleCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
